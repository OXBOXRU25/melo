<?php
/**
 * Каталог, отфильтрованный по направлению.
 *
 * Адрес вида /projects/napravlenie/landshaft-i-maf/ должен показывать тот
 * же каталог, что и /projects/, только с проектами одного направления.
 * Разметка полностью совпадает, поэтому отдельного шаблона тут не нужно —
 * подключаем архив проектов. Заголовок страницы WordPress подставит сам:
 * на архиве это «Кейсы», здесь — название направления.
 *
 * @package oxboxwise
 */

require get_template_directory() . '/archive-projects.php';
